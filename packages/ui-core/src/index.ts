/**
 * @daak/ui-core — not yet implemented.
 *
 * Planned exports: createCommandRegistry(), createKeymap(), message list and thread view models.
 *
 * See ./CLAUDE.md for scope, allowed imports, and done-criteria before
 * starting this lane.
 */
export {};
