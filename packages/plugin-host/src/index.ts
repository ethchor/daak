/**
 * @daak/plugin-host — not yet implemented.
 *
 * Planned exports: createPluginHost({ api }), loadManifest(path).
 *
 * See ./CLAUDE.md for scope, allowed imports, and done-criteria before
 * starting this lane.
 */
export {};
