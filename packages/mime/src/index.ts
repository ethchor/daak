/**
 * @daak/mime — not yet implemented.
 *
 * Planned exports: parseMessage(bytes), buildMessage(draft), decodeHeader(value), MessageStructure.
 *
 * See ./CLAUDE.md for scope, allowed imports, and done-criteria before
 * starting this lane.
 */
export {};
