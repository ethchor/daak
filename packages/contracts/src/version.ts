/**
 * Bumped whenever a persisted shape or a seam interface changes in a way that
 * an older package could not read. Locked after week 0 — a change here is a
 * conversation, not a commit (see CONTRIBUTING.md).
 */
export const CONTRACTS_VERSION = 1;
