/**
 * @daak/store — not yet implemented.
 *
 * Planned exports: openStore(driver), migrate(), rebuildProjections(), typed query helpers.
 *
 * See ./CLAUDE.md for scope, allowed imports, and done-criteria before
 * starting this lane.
 */
export {};
